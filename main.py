from class3 import RuneKnight
from tools import SimsTool


if __name__ == '__main__':
    # SimsTool.get_rune_mastery_best_status(level=107, is_transcended=True)
    SimsTool.get_poison_creation_best_status(level=107, is_transcended=True)
